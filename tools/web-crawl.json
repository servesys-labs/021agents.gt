{
  "name": "web-crawl",
  "description": "Crawl a website and return content as clean markdown. Fast, respects robots.txt, perfect for RAG ingestion and research. No browser needed — use browser-render only when this is blocked.",
  "input_schema": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "URL to crawl"
      },
      "max_pages": {
        "type": "integer",
        "description": "Maximum pages to crawl (default: 5)",
        "default": 5
      },
      "max_depth": {
        "type": "integer",
        "description": "Maximum crawl depth from start URL (default: 1)",
        "default": 1
      },
      "format": {
        "type": "string",
        "enum": ["markdown", "html", "json"],
        "description": "Output format (default: markdown)",
        "default": "markdown"
      }
    },
    "required": ["url"]
  }
}
