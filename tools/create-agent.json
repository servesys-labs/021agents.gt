{
  "name": "create-agent",
  "description": "Create a new agent definition from a description. Generates a complete agent config (name, system prompt, tools, governance) and saves it to agents/.",
  "input_schema": {
    "type": "object",
    "properties": {
      "description": {
        "type": "string",
        "description": "What the agent should do — be specific about its purpose, tools, and constraints"
      },
      "name": {
        "type": "string",
        "description": "Agent name (lowercase, hyphenated). If omitted, generated from description."
      }
    },
    "required": ["description"]
  }
}
