{
  "name": "evolve-agent",
  "description": "Analyze an agent's session history, identify failure patterns, and generate improvement proposals. Returns analysis report with recommendations.",
  "input_schema": {
    "type": "object",
    "properties": {
      "agent_name": {
        "type": "string",
        "description": "Name of the agent to analyze and evolve"
      },
      "action": {
        "type": "string",
        "enum": ["analyze", "propose", "status"],
        "description": "Action: 'analyze' runs failure analysis, 'propose' generates improvement proposals, 'status' shows evolution timeline"
      }
    },
    "required": ["agent_name"]
  }
}
