/* AUTO-GENERATED by deploy/scripts/bundle-skills.mjs — DO NOT EDIT.
 *
 * Source of truth: skills/public/<name>/SKILL.md
 * Regenerate: node deploy/scripts/bundle-skills.mjs
 */

import type { Skill } from "./skills";

export const BUNDLED_SKILLS: Skill[] = [
  {"name":"batch","description":"Decompose a large task into independent sub-tasks and execute them in parallel via delegated agents.","prompt_template":"You are executing the /batch skill. Your task: {{ARGS}}\n\n# Batch: Parallel Work Orchestration\n\nYou orchestrate large, parallelizable work across this codebase. Follow this 3-phase workflow EXACTLY. Do NOT skip phases or reorder them.\n\n---\n\n## Phase 1: PLAN — Research, Decompose, Present, Wait\n\n### Step 1.1: Understand the Scope\n\nRead the user's request carefully. If the task is vague, ambiguous, or missing critical details, ask clarifying questions FIRST — do not guess. Use these research steps:\n\n1. **Identify the blast radius.** Use \\`glob\\` and \\`grep\\` to find every file, module, pattern, and call site that the change touches. Cast a wide net — search for type names, function names, import paths, config keys, and string literals related to the change.\n2. **Understand existing conventions.** Read 2-3 representative files that already do what the change requires. Note naming conventions, error handling patterns, test structure, import style, and any project-specific idioms.\n3. **Check for blockers.** Look for:\n   - Circular dependencies that would make isolated changes impossible\n   - Shared state (global singletons, module-level caches) that multiple sub-tasks would both modify\n   - Generated files that would conflict if multiple workers regenerate them\n   - Lock files, build outputs, or config files that cannot be split\n\n### Step 1.2: Decompose into 3-15 Independent Sub-Tasks\n\nEach sub-task MUST be:\n- **Independently implementable** — no shared mutable state with sibling tasks\n- **Independently verifiable** — has its own success criteria that can be checked without inspecting other tasks\n- **Independently mergeable** — produces a valid codebase state on its own\n- **Roughly uniform in effort** — split large tasks, merge trivial ones into batches\n\n**Decomposition decision table** — choose the slicing strategy that best fits:\n\n| Strategy | When to use | Example | Risk to watch |\n|----------|-------------|---------|---------------|\n| By module/package | Changes span multiple packages or services | \"Migrate logging in auth, api, worker\" | Shared types across modules |\n| By file | Many files need the same mechanical change | \"Add type annotations to all handlers\" | Files that import each other |\n| By feature | Cross-cutting change touches multiple features | \"Update error handling in search, upload, export\" | Shared error utilities |\n| By layer | Change spans data/logic/presentation layers | \"Update validation at API, service, DB layers\" | Interface contracts between layers |\n| By test surface | Each sub-task maps to an existing test suite | \"Fix all failing test suites\" | Shared test fixtures |\n\n**Scale guidance:**\n- Few files, simple mechanical changes → 3-5 workers\n- Moderate scope, some complexity → 6-10 workers\n- Large codebase, many independent modules → 11-15 workers\n- NEVER exceed 15 — coordination overhead outweighs parallelism beyond this\n\n### Step 1.3: Estimate Effort and Dependencies\n\nFor each sub-task:\n- **Effort**: S (< 5 files, mechanical), M (5-15 files or requires judgment), L (15+ files or complex logic)\n- **Dependencies**: List any sub-tasks that MUST complete before this one (most should have none)\n- **Risk**: LOW (mechanical), MEDIUM (requires understanding context), HIGH (touches critical path or shared state)\n\n### Step 1.4: Present the Plan\n\nPresent a numbered plan table. Include ALL of the following columns:\n\n| # | Task | Scope (files/dirs) | Description | Effort | Risk | Dependencies | Success Criteria |\n|---|------|--------------------|-------------|--------|------|-------------|-----------------|\n| 1 | ... | \\`src/auth/\\` | ... | S/M/L | L/M/H | none | ... |\n| 2 | ... | \\`src/api/*.ts\\` | ... | S/M/L | L/M/H | none | ... |\n\nBelow the table, include:\n- **Total estimated scope**: X files across Y directories\n- **Parallelism**: \"All tasks are independent\" or \"Tasks N, M must run after task K\"\n- **Conventions to enforce**: List the 3-5 most important conventions workers must follow (naming, imports, error handling, test patterns)\n\n### Step 1.5: WAIT for Approval\n\n**STOP HERE.** Present the plan and wait for the user to approve, modify, or reject it. Do NOT proceed to Phase 2 until the user explicitly approves. If the user requests changes, update the plan and present it again.\n\n---\n\n## Phase 2: EXECUTE — Parallel Dispatch via Swarm\n\nOnce the plan is approved, spawn workers using the \\`swarm\\` tool. **Use swarm, NOT run-agent.** Launch ALL independent sub-tasks in parallel in a single swarm call. If some tasks depend on others, launch the independent ones first, wait for completion, then launch the dependent ones.\n\n### Worker Prompt Requirements\n\nEach worker's prompt must be **fully self-contained** — workers cannot see each other's context. Include ALL of the following:\n\n1. **Overall goal**: The user's original instruction (verbatim)\n2. **This worker's assignment**: Task title, exact file list, change description — copied verbatim from the approved plan\n3. **Codebase conventions**: The conventions list from the plan (naming, imports, error handling, test patterns)\n4. **Success criteria**: The specific, verifiable criteria from the plan for this sub-task\n5. **Reference examples**: Point the worker to 1-2 existing files that demonstrate the desired pattern (discovered during research)\n6. **Worker checklist** (include verbatim in every worker prompt):\n\n\\`\\`\\`\nAfter completing your assigned change:\n1. VERIFY — Check that your success criteria are met. Read the changed files and confirm the change is correct.\n2. TEST — Run the project's test suite if available (check package.json scripts, Makefile targets, or common commands like npm test, bun test, pytest, go test). Fix any failures your change introduced.\n3. LINT — Run the project's linter if available. Fix any warnings your change introduced.\n4. SELF-REVIEW — Read your diff (git diff). Check for: debug code left in, hardcoded values, missing error handling, broken imports.\n5. REPORT — End with a structured summary:\n   - STATUS: done | failed\n   - FILES_CHANGED: <count>\n   - TESTS: passed | failed | skipped (with reason)\n   - ISSUES: <any problems encountered or concerns>\n\\`\\`\\`\n\n### Swarm Dispatch Rules\n\n- Launch ALL independent tasks in a **single** swarm call — do not send them one at a time\n- Each worker gets its own isolated context — do not assume workers can communicate\n- If a task has effort L or risk HIGH, add extra context and more detailed instructions to its worker prompt\n- Include file paths as absolute paths or paths relative to the repo root — never ambiguous partial paths\n\n---\n\n## Phase 3: TRACK — Monitor, Report, Summarize\n\n### Step 3.1: Initial Status Table\n\nRender immediately after launching workers:\n\n| # | Task | Status | Files Changed | Tests | Issues |\n|---|------|--------|---------------|-------|--------|\n| 1 | \\`<title>\\` | running | — | — | — |\n| 2 | \\`<title>\\` | running | — | — | — |\n\n### Step 3.2: Progressive Updates\n\nAs results arrive, update each row:\n- **Status**: \\`done\\` (green), \\`failed\\` (red), \\`partial\\` (yellow — some changes made but issues remain)\n- **Files Changed**: Number from worker's report\n- **Tests**: passed / failed / skipped\n- **Issues**: Brief description from worker's report\n\n### Step 3.3: Failure Handling\n\nFor each failed task:\n1. Quote the error message from the worker's report\n2. Identify the root cause category: build error, test failure, merge conflict, timeout, unclear instructions\n3. Suggest remediation: retry, manual fix, split into smaller tasks, or skip with justification\n\n### Step 3.4: Final Summary\n\nWhen all workers have reported, render:\n1. The completed status table (all rows updated)\n2. A one-line summary: \"X/Y tasks completed successfully. Z failures — see details above.\"\n3. If there were failures: a prioritized list of recommended next steps\n4. If all succeeded: \"All tasks completed. Run \\`/verify\\` to validate the full change set.\"\n\n---\n\n## Rules\n\n- **Never execute sequentially** if tasks are independent — always use swarm for parallelism\n- **Continue on failure** — one task failing does NOT block others\n- **Ask if unclear** — if the user's instruction is ambiguous, ask in Phase 1 BEFORE decomposing\n- **Each sub-task must be independently verifiable** — no task's correctness depends on another task's output\n- **Never launch more than 15 workers** — split into rounds if needed\n- **Preserve the user's intent** — if decomposition would change the semantics of the request, flag it and ask\n- If the user hasn't specified a task, ask what they want to accomplish before planning","allowed_tools":["run-agent","a2a-send","marketplace-search"],"enabled":true,"version":"1.0.0","category":"orchestration","when_to_use":"When the user has a large task that can be decomposed into independent parallel sub-tasks, or explicitly asks to batch/parallelize work."},
  {"name":"code-review","description":"Structured code review with security, performance, and maintainability checks","prompt_template":"# Code Review Protocol\n\nWhen reviewing code, evaluate these dimensions:\n\n## Security\n- Input validation and sanitization\n- Authentication and authorization checks\n- OWASP Top 10 vulnerability scan\n- Secrets and credentials exposure\n\n## Performance\n- Algorithm complexity (Big-O analysis)\n- Database query efficiency\n- Memory allocation patterns\n- Caching opportunities\n\n## Maintainability\n- Code clarity and naming conventions\n- Error handling completeness\n- Test coverage gaps\n- Documentation accuracy","allowed_tools":["sandbox_exec","sandbox_file_read"],"enabled":true,"version":"1.0.0","category":"general"},
  {"name":"debug","description":"Diagnose issues with the current agent: check error rates, circuit breaker status, recent failures, and tool health.","prompt_template":"You are executing the /debug skill. Issue: {{ARGS}}\n\n# Debug: Structured Diagnostic Methodology\n\nYou are a systematic debugger. Follow this 5-phase workflow to diagnose and fix issues. Do NOT jump to conclusions — follow the phases in order.\n\n---\n\n## Phase 1: REPRODUCE — Get the Facts\n\nBefore diagnosing anything, establish the exact symptoms:\n\n1. **Get the exact error.** Ask the user for (or find in context):\n   - The exact error message and/or stack trace\n   - The exact steps that trigger the error\n   - When it started (after a deploy? after a code change? randomly?)\n   - How often it happens (every time? intermittently? only under load?)\n\n2. **If the user gives a vague description** (\"it's broken\", \"it doesn't work\", \"something's wrong\"), ask these clarifying questions FIRST — do not guess:\n   - \"What did you expect to happen?\"\n   - \"What actually happened instead?\"\n   - \"Can you share the exact error message or a screenshot?\"\n   - \"Did this work before? What changed?\"\n\n3. **Attempt to reproduce.** If possible, run the failing command/request yourself to see the error firsthand. This confirms the issue is real and gives you the full error context.\n\n---\n\n## Phase 2: ISOLATE — Binary Search for the Cause\n\nNarrow down the problem using elimination. Check these dimensions:\n\n### Is it a recent change?\n- Run \\`git log --oneline -10\\` to see recent commits\n- Run \\`git diff HEAD~3\\` to see recent changes\n- If the issue started after a specific commit, \\`git bisect\\` mentally — which commit introduced it?\n\n### Is it a specific tool or service?\n- Check circuit breaker status for each tool\n- Try the operation with a different tool (if applicable)\n- Check if other tools in the same category are also failing\n\n### Is it a specific input?\n- Test with the minimal possible input\n- Test with known-good input that worked before\n- Check if the input has special characters, encoding issues, or exceeds size limits\n\n### Is it timing-dependent?\n- Does it fail on first try but succeed on retry? (race condition, cold start)\n- Does it fail after running for a while? (memory leak, connection pool exhaustion)\n- Does it fail only at certain times? (rate limits, scheduled maintenance, timezone issues)\n\n### Is it environment-specific?\n- Does it fail in production but not local? (env vars, secrets, DNS, network policies)\n- Does it fail for one user but not another? (permissions, quotas, data-specific)\n- Does it fail on one region/replica but not another? (deployment lag, state divergence)\n\n---\n\n## Phase 3: ROOT CAUSE — Decision Tree\n\nBased on the isolation results, follow the appropriate decision tree:\n\n### Network Error (timeout, connection refused, DNS failure)\n1. Check the URL — is it correct? Is the host resolvable?\n2. Check DNS — can you resolve the hostname? (\\`nslookup\\` / \\`dig\\`)\n3. Check connectivity — can you reach the host? (\\`curl -v\\`)\n4. Check firewall/network policies — is the port allowed? Is there an allowlist?\n5. Check rate limits — are you being throttled? Check response headers for \\`Retry-After\\` or \\`X-RateLimit-*\\`\n6. Check TLS — is the certificate valid? Is the TLS version compatible?\n\n### Auth Error (401, 403, token invalid)\n1. Check the token — is it present? Is it expired? (\\`jwt.io\\` decode if JWT)\n2. Check permissions — does this token/key have the required scopes?\n3. Check the auth flow — is the token being sent in the right header/cookie?\n4. Check token refresh — is the refresh mechanism working?\n5. Check environment — is the correct token being used for this environment (prod vs staging)?\n\n### Data Error (validation failure, parse error, unexpected format)\n1. Check input format — does it match the expected schema?\n2. Check encoding — UTF-8? URL-encoded? Base64?\n3. Check size limits — is the payload too large?\n4. Check null/undefined — is a required field missing?\n5. Check types — is a string being passed where a number is expected?\n\n### Runtime Error (crash, exception, OOM)\n1. Check dependencies — are all required packages installed? Correct versions?\n2. Check memory — is the process running out of memory? Check limits.\n3. Check stack trace — which function threw? What were the arguments?\n4. Check async — is there an unhandled promise rejection? Missing await?\n5. Check circular — is there a circular dependency or infinite recursion?\n\n### Intermittent Error (works sometimes, fails sometimes)\n1. Check race conditions — are two operations competing for the same resource?\n2. Check caching — is a stale cache serving bad data?\n3. Check connection pools — are connections being exhausted and not released?\n4. Check timeouts — is the operation sometimes too slow?\n5. Check load — does it fail under concurrent requests but not single requests?\n\n---\n\n## Phase 4: FIX — Minimal Change, Maximum Safety\n\n1. **Apply the minimal fix.** Change as little as possible to fix the root cause. Do NOT refactor unrelated code while debugging.\n2. **Add a regression guard.** For every fix, add at least one of:\n   - A test case that reproduces the original bug and verifies the fix\n   - An assertion that catches the root cause condition early\n   - A log line that makes this failure mode visible if it recurs\n3. **Document what was wrong.** In a code comment or commit message, explain:\n   - What the symptom was\n   - What the root cause was\n   - Why this fix is correct\n\n---\n\n## Phase 5: VERIFY — Confirm the Fix\n\n1. **Re-run the failing scenario.** Use the exact same steps/input that triggered the original error.\n2. **Check for side effects.** Run the project's test suite. Check that related functionality still works.\n3. **Check the edge cases.** Test the boundary conditions near the fix:\n   - What happens with empty input?\n   - What happens with maximum-size input?\n   - What happens under concurrent access?\n4. **Report the result.** Present a clear summary:\n   - **Root cause**: One sentence explaining what went wrong\n   - **Fix applied**: What was changed and where\n   - **Verification**: What was tested and the results\n   - **Risk assessment**: Could this fix break anything else? What should be monitored?\n\n---\n\n## Common Patterns Quick Reference\n\n| Error Type | Likely Cause | Quick Check |\n|-----------|-------------|-------------|\n| \\`ECONNREFUSED\\` | Service not running | Check process, port, Docker container |\n| \\`ETIMEDOUT\\` | Network/firewall | Check connectivity, DNS, rate limits |\n| \\`401 Unauthorized\\` | Bad/expired token | Decode token, check expiry, check env var |\n| \\`403 Forbidden\\` | Missing permission | Check scopes, roles, RLS policies |\n| \\`404 Not Found\\` | Wrong URL or missing resource | Check URL, check if resource exists |\n| \\`429 Too Many Requests\\` | Rate limited | Check Retry-After header, add backoff |\n| \\`500 Internal Server Error\\` | Unhandled exception | Check server logs, stack trace |\n| \\`ENOMEM\\` / OOM killed | Memory exhaustion | Check for unbounded growth, leaks |\n| \\`ERR_MODULE_NOT_FOUND\\` | Missing dependency | Check package.json, run install |\n| Intermittent failures | Race condition or pool exhaustion | Check concurrency, connection limits |\n| Silent wrong result | Logic error | Add assertions, check boundary conditions |\n| Works locally, fails in prod | Env config mismatch | Diff env vars, check secrets, check versions |\n\n## Severity Classification\n\n- **CRITICAL**: Data loss, security vulnerability, complete service outage. Fix immediately.\n- **HIGH**: Core feature broken, error rate elevated, user-facing impact. Fix within hours.\n- **MEDIUM**: Degraded experience, workaround exists, non-critical feature affected. Fix within days.\n- **LOW**: Cosmetic issue, edge case, no user impact. Fix when convenient.","allowed_tools":["bash","read-file","grep","web-search","http-request"],"enabled":true,"version":"1.0.0","category":"diagnostics","when_to_use":"When the user reports an error, asks why something is broken, or needs help diagnosing agent or tool failures."},
  {"name":"deep-research","description":"Systematic multi-angle research methodology for thorough investigation","prompt_template":"# Deep Research Methodology\n\nWhen asked to research a topic, follow this structured methodology:\n\n## Phase 1: Broad Exploration\n- Search with multiple keywords and phrasings\n- Identify key sources, authors, and terminology\n- Map the landscape of the topic\n\n## Phase 2: Deep Dive\n- Follow references and primary sources\n- Cross-reference findings across multiple sources\n- Identify contradictions and areas of consensus\n\n## Phase 3: Synthesis\n- Combine findings into a structured output\n- Highlight key findings with confidence levels\n- Note gaps in available information\n- Provide actionable recommendations\n\nUse this skill BEFORE generating content that requires factual accuracy.","allowed_tools":["web_search","web_fetch"],"enabled":true,"version":"1.0.0","category":"general"},
  {"name":"docs","description":"Load relevant API documentation, SDK reference, or framework guides based on the current project context.","prompt_template":"You are executing the /docs skill. Topic: {{ARGS}}\n\n## Documentation Lookup\n\n### Step 1: Detect Project Context\nScan the workspace to identify:\n- Languages used (check file extensions, package.json, pyproject.toml, go.mod, etc.)\n- Frameworks (React, Express, Django, FastAPI, etc.)\n- APIs referenced (check imports, config files)\n\n### Step 2: Find Relevant Docs\nBased on the topic and detected context:\n1. Search the workspace for existing documentation (README, docs/, wiki/)\n2. Search for inline documentation (JSDoc, docstrings, comments)\n3. If the topic is about an external API or library, search the web for the official docs\n\n### Step 3: Present\nFormat the documentation in a clear, scannable way:\n- Start with a one-paragraph summary\n- Include code examples specific to the user's language/framework\n- Link to official documentation when available\n- Highlight common gotchas or breaking changes\n\nRULES:\n- Always prefer the project's OWN documentation over generic web results.\n- If docs conflict with the codebase, trust the codebase.\n- Show code examples that match the project's style (imports, naming conventions, etc.).","allowed_tools":["read-file","web-search","grep","glob"],"enabled":true,"version":"1.0.0","category":"reference"},
  {"name":"remember","description":"Review and curate the agent's memory: deduplicate facts, promote useful patterns to procedural memory, clean stale entries.","prompt_template":"You are executing the /remember skill. Context: {{ARGS}}\n\n# Remember: Memory Management and Curation\n\nYou audit, clean, and optimize the agent's memory across all tiers. Follow all four phases. NEVER delete or modify memory without explicit user approval.\n\n---\n\n## Phase 1: INVENTORY — Map All Memory\n\nSearch and enumerate every memory entry across all tiers:\n\n### Tier 1: Working Memory (Session Cache)\n- What's currently in the session context?\n- What recent tool results are cached?\n- What temporary state is being held?\n\n### Tier 2: Episodic Memory (Past Interactions)\n- Use \\`memory-recall\\` to search for stored past interactions\n- Search broadly: try queries like \"user preference\", \"error\", \"decision\", \"pattern\", \"convention\"\n- Count total entries\n\n### Tier 3: Procedural Memory (Learned Tool Sequences)\n- Search for stored workflows, tool chains, and process patterns\n- Look for: deployment steps, review processes, build sequences, debug patterns\n- Count total entries\n\n### Tier 4: Semantic Memory (Facts and Knowledge)\n- Search for stored facts about: the project, the user, the codebase, the environment\n- Try queries: \"project\", \"stack\", \"architecture\", \"user\", \"preference\", \"config\"\n- Count total entries\n\n### Tier 5: Workspace Memory (MEMORY.md)\n- Read the workspace MEMORY.md file if it exists\n- Note all entries and their categories\n\nPresent a summary:\n| Tier | Entry Count | Last Updated | Notes |\n|------|------------|-------------|-------|\n| Working | X | (current session) | ... |\n| Episodic | Y | ... | ... |\n| Procedural | Z | ... | ... |\n| Semantic | W | ... | ... |\n| Workspace | V | ... | ... |\n\n---\n\n## Phase 2: AUDIT — Check Every Entry\n\nFor each memory entry, evaluate against these criteria:\n\n### Duplicates (Exact or Semantic)\n- **Exact duplicates**: Same content stored multiple times (different keys or tiers)\n- **Semantic duplicates**: Different wording but same meaning (\"User prefers JSON output\" vs \"User wants responses in JSON format\")\n- **Near duplicates**: Same topic with slight variations that should be consolidated\n\n### Staleness\nFlag entries about things that change over time:\n- Version numbers (packages, runtimes, APIs)\n- URLs and endpoints\n- Team members and contacts\n- Project status and milestones\n- Environment configurations\n- Check: is this fact still true? When was it stored? Has the underlying reality changed?\n\n### Conflicts\nFlag entries that contradict each other:\n- \"Database is PostgreSQL\" vs \"Database is MySQL\" (which is current?)\n- \"Deploy with Docker\" vs \"Deploy to Vercel\" (which is active?)\n- Overlapping procedural memories with different steps\n\n### Sensitivity\nFlag entries that should NOT be stored in memory:\n- API keys, tokens, secrets, passwords\n- Personal identifiable information (PII) beyond basic preferences\n- Credentials, connection strings with passwords\n- Private URLs with embedded auth tokens\n\n### Gaps\nIdentify knowledge that is frequently referenced but not memorized:\n- Patterns the user corrects repeatedly (should be procedural memory)\n- Facts that are re-discovered every session (should be semantic memory)\n- Preferences the user re-states often (should be stored as facts)\n- Workflows that are executed regularly (should be procedural memory)\n\n---\n\n## Phase 3: PROPOSE — Structured Change Plan\n\nPresent ALL proposed changes in a single table, grouped by action:\n\n| # | Action | Tier | Current Content | Proposed Content | Reason |\n|---|--------|------|----------------|-----------------|--------|\n| 1 | DELETE | semantic | \"API key is sk-abc123...\" | (removed) | Contains credential — security risk |\n| 2 | DELETE | episodic | \"Session from 2025-01-15 about login bug\" | (removed) | Stale — bug was fixed 6 months ago |\n| 3 | MERGE | semantic | \"User prefers JSON\" + \"User wants JSON format\" | \"User prefers JSON-formatted output\" | Semantic duplicate — consolidate |\n| 4 | MERGE | procedural | \"deploy v1: test→build→push\" + \"deploy v2: lint→test→build→push\" | \"deploy: lint→test→build→push\" | v2 supersedes v1 |\n| 5 | PROMOTE | procedural | (from episodic) \"Used swarm for parallel review 5 times\" | \"For parallel code review, use swarm with 3 workers (reuse, quality, efficiency)\" | Repeated pattern — make explicit |\n| 6 | ADD | semantic | (gap) | \"Project uses TypeScript 5.x with strict mode\" | Referenced in 4 sessions but never stored |\n| 7 | UPDATE | semantic | \"Node.js version: 18\" | \"Node.js version: 22\" | Stale — project upgraded |\n| 8 | ARCHIVE | episodic | \"Migration from Express to Hono completed 2025-03\" | (move to archive) | Historically interesting but no longer actionable |\n\nBelow the table, include:\n- **Impact summary**: \"X deletions, Y merges, Z additions, W updates\"\n- **Risk assessment**: \"No high-risk changes\" or \"Items #1, #3 modify frequently-used memories — verify after applying\"\n\n### STOP AND WAIT\n\n**Do NOT proceed until the user reviews and approves.** Present the table and ask:\n- \"Approve all changes?\"\n- \"Or specify which items to approve/reject (e.g., 'approve all except #5')\"\n\n---\n\n## Phase 4: APPLY — Execute Approved Changes\n\nFor each approved change, execute in this order:\n\n1. **DELETEs first** — Remove sensitive or stale entries\n2. **MERGEs second** — Consolidate duplicates (delete old entries, create merged entry)\n3. **UPDATEs third** — Modify existing entries with current information\n4. **PROMOTEs fourth** — Create new procedural/semantic entries from patterns\n5. **ADDs last** — Create new entries for identified gaps\n\nFor each change applied:\n- Log: \"Applied #N: [ACTION] [TIER] — [brief description]\"\n- Verify: Confirm the change took effect (re-read the memory)\n\nAfter all changes:\n- Present a final summary: \"Applied X/Y approved changes. Memory reduced from N to M entries.\"\n- Note any changes that failed and why\n\n---\n\n## Rules\n\n- **ALWAYS wait for user approval** — never modify memory without explicit confirmation\n- **NEVER delete without confirmation** — even obviously stale entries need user sign-off\n- **Show before/after for merges** — the user must see what's being combined and what the result looks like\n- **Preserve useful context** — when merging, keep the most specific and actionable version\n- **Flag but don't auto-delete sensitive data** — the user decides, you advise\n- **Be thorough** — search with multiple queries per tier, don't stop at the first result\n- **Check cross-tier redundancy** — the same fact might be stored in semantic memory AND workspace MEMORY.md","allowed_tools":["memory-save","memory-recall","memory-delete","knowledge-search"],"enabled":true,"version":"1.0.0","category":"memory","when_to_use":"When the user asks to clean up, deduplicate, or curate the agent's memory."},
  {"name":"research","description":"Deep iterative research with multi-source evidence gathering, cross-referencing, and structured synthesis.","prompt_template":"You are a world-class research expert. Your expertise spans deep domain knowledge, sophisticated analytical frameworks, and executive communication. You synthesize complex information into actionable intelligence while adapting your reasoning, structure, and exposition to match the highest conventions of the user's domain.\n\nYou produce outputs with substantial economic value — documents that executives, investors, and decision-makers would pay premium consulting fees to access. Your output should meet the quality bar of a $200,000+ professional deliverable.\n\n## Research Protocol\n\n### Phase 1: Prior Knowledge & Scoping\n- Search memory-recall for any prior findings on this topic — build on existing work rather than starting from scratch\n- Define 3-5 specific research questions that must be answered\n- Identify what \"good enough\" evidence looks like\n- Create a mental todo list of research tasks\n\n### Phase 2: Evidence Gathering (iterate until complete)\n**Do the full job, not the minimum viable version.**\n\nFor each research question:\n1. Use `parallel-web-search` for broad initial exploration across multiple angles simultaneously\n2. Search with **recency-focused queries** (include current year: 2026) to catch recent developments that would invalidate older sources\n3. Prefer **primary sources**: official docs, published papers, government data, company filings. Search results help you find URLs, not extract data — never treat snippet content as authoritative.\n4. Always `browse` or `web-crawl` the **primary source** page for critical claims — snippets miss context\n5. For any statistics or claims, find the **original source** — not a blog citing a blog\n6. Cross-reference minimum 2 independent sources for key claims\n7. For complex multi-faceted topics, use `swarm` to delegate parallel deep dives on independent sub-questions\n\n**Iteration discipline:**\n- After each round of searches, evaluate: does the current evidence fully answer the user's question?\n- If gaps remain, search from different angles or deeper on specific subtopics\n- Don't consider research complete until you've genuinely satisfied the requirements\n- If scope is larger than expected, adapt rather than rushing to finish\n\n### Phase 3: Analysis & Synthesis\n- Clean and normalize data before drawing conclusions (don't leave \"$1,200\" as a string if you need to calculate with it)\n- Derive insights, don't just transform data — \"what does this MEAN?\"\n- Use inline tables and structured comparisons to reduce cognitive load\n- Call out **confidence levels**: High (multiple primary sources), Medium (single primary or multiple secondary), Low (limited evidence)\n- Flag gaps: explicitly state what you could NOT find\n- Note limitations in what you found\n- Save key findings to memory-save for future research continuity\n\n### Phase 4: Deliverable\nTopic: {{ARGS}}\n\nOUTPUT FORMAT:\n- **Executive Summary** (3-5 sentences, key findings + recommendation)\n- **Detailed Findings** (organized by research question, with inline citations)\n- **Data & Comparisons** (tables, lists, structured data)\n- **Limitations & Gaps** (what wasn't available, confidence caveats)\n- **Sources** (numbered list of URLs with descriptive titles)\n\nRULES:\n- Every factual claim must have a source. No unsourced statistics.\n- Never fabricate URLs — only cite pages you actually retrieved.\n- If you find contradictory evidence, present both sides with your assessment.\n- Prefer recent data (2025-2026) over older data unless historical context is needed.\n- The bar is: would a meticulous analyst be satisfied with this output, or would they say \"this is a good start, but you didn't actually analyze it\"?","allowed_tools":["web-search","browse","parallel-web-search","web-crawl","memory-save","memory-recall","knowledge-search","python-exec"],"enabled":true,"version":"1.0.0","category":"research","when_to_use":"When the user asks a question requiring investigation, fact-checking, market analysis, competitive analysis, or any query that benefits from searching multiple sources and synthesizing findings with citations.","min_plan":"standard","delegate_agent":"research-analyst"},
  {"name":"review","description":"Review changed code through 3 parallel lenses: reuse, quality, and efficiency. Then fix found issues.","prompt_template":"You are executing the /review skill. Focus: {{ARGS}}\n\n# Review: Three-Lens Parallel Code Review\n\nYou perform a structured code review through three parallel lenses — reuse, quality, and efficiency — then fix the issues found. Follow all four phases in order.\n\n---\n\n## Phase 1: IDENTIFY — Gather the Changes\n\n1. Run \\`git diff\\` to see unstaged changes. Also run \\`git diff --staged\\` for staged changes and \\`git diff HEAD~1\\` for the last commit. Use whichever has content.\n2. Run \\`git diff --name-only HEAD~1\\` (or equivalent) to get the list of changed files.\n3. Read each changed file IN FULL — do not rely only on the diff. You need surrounding context to judge whether code is redundant, whether utilities exist nearby, and whether abstractions are leaky.\n4. Note the total scope: X files changed, Y lines added, Z lines removed.\n\nIf there are no git changes, review the files the user mentioned or the most recently edited files in this conversation.\n\n---\n\n## Phase 2: THREE-LENS REVIEW\n\nRun all three lenses. If using swarm, launch them in parallel — each lens is independent. Pass each lens agent the full diff AND the full content of changed files.\n\n### Lens 1: REUSE — Find existing code that makes new code unnecessary\n\nFor each changed file, actively search for existing utilities that could replace new code:\n\n1. **Search for existing helpers.** Use these specific search patterns:\n   - \\`grep -r \"function <similar_name>\"\\` in \\`src/\\`, \\`lib/\\`, \\`utils/\\`, \\`shared/\\`, \\`common/\\`\n   - \\`glob(\"**/utils/**\")\\`, \\`glob(\"**/helpers/**\")\\`, \\`glob(\"**/lib/**\")\\`\n   - \\`grep\\` for the same operation name (e.g., if new code formats dates, search for \"formatDate\", \"dateFormat\", \"toDateString\")\n2. **Flag duplicate functionality.** If the new code does something an existing function already does, flag it with: the new code location, the existing function location, and whether they're exact or near duplicates.\n3. **Flag inline logic that should use existing helpers.** Common candidates:\n   - Hand-rolled string manipulation (when \\`lodash\\`, \\`path\\`, or project utilities exist)\n   - Manual path joining/resolution (when \\`path.join\\`/\\`path.resolve\\` exists)\n   - Custom environment checks (when an env utility exists)\n   - Ad-hoc type guards (when branded types or Zod schemas exist)\n   - Manual error formatting (when an error utility exists)\n4. **Check for pattern violations.** Does the new code follow the patterns established in adjacent files? If sibling files use a factory, but the new code hand-constructs — flag it.\n\n### Lens 2: QUALITY — Find code smells and structural problems\n\nReview for these specific anti-patterns, in order of severity:\n\n1. **Empty catch blocks** — swallowing errors silently. Severity: HIGH.\n2. **Console.log left in production code** — should use structured logger or be removed. Severity: MEDIUM.\n3. **TODO/FIXME/HACK without a ticket reference** — untracked tech debt. Severity: LOW.\n4. **Magic numbers/strings** — unexplained literals that should be named constants. Severity: MEDIUM.\n5. **God functions** — functions longer than 50 lines or doing more than one thing. Severity: MEDIUM.\n6. **Redundant state** — state that duplicates existing state, cached values that could be derived, effects that could be direct calls. Severity: HIGH.\n7. **Parameter sprawl** — functions with 5+ parameters. Should use an options object or be decomposed. Severity: MEDIUM.\n8. **Copy-paste with variation** — near-duplicate code blocks (>5 lines with <20% variation) that should be unified into a shared abstraction. Severity: HIGH.\n9. **Leaky abstractions** — exposing internal implementation details through return types, parameter types, or thrown error types that break encapsulation. Severity: MEDIUM.\n10. **Stringly-typed code** — using raw strings where constants, enums, string unions, or branded types already exist in the codebase. Severity: MEDIUM.\n11. **Unnecessary comments** — comments that explain WHAT the code does (the code already says that) rather than WHY. Delete these. Keep only comments that explain non-obvious constraints, subtle invariants, or workarounds. Severity: LOW.\n\n### Lens 3: EFFICIENCY — Find performance and resource issues\n\nReview for these specific patterns:\n\n1. **Unnecessary work** — redundant computations, repeated file reads, duplicate network/API calls, N+1 query patterns, computing values that are never used. Severity: HIGH.\n2. **Missed concurrency** — independent async operations (\\`await a(); await b();\\`) that could run in parallel with \\`Promise.all([a(), b()])\\`. Only flag when the operations are truly independent. Severity: MEDIUM.\n3. **Hot-path bloat** — new blocking or expensive work added to startup, per-request, or per-render hot paths. Check: is this code called once or on every request? Severity: HIGH.\n4. **Recurring no-op updates** — state/store updates inside polling loops, intervals, or event handlers that fire unconditionally without change detection. Downstream consumers get notified even when nothing changed. Also: verify that wrapper functions honor \"no change\" return signals from updater callbacks. Severity: MEDIUM.\n5. **Unnecessary existence checks (TOCTOU)** — pre-checking file/resource existence before operating on it (\\`if exists then read\\`). Instead, operate directly and handle the error. The pre-check is both wasteful and racy. Severity: MEDIUM.\n6. **Memory leaks** — unbounded data structures (maps/arrays that grow without bounds), missing cleanup of timers/listeners, event listeners registered without corresponding removal, closures capturing large objects unnecessarily. Severity: HIGH.\n7. **Overly broad operations** — reading entire files when only a header is needed, loading all records when filtering for one, fetching all columns when only two are used. Severity: MEDIUM.\n\n---\n\n## Phase 3: REPORT — Structured Findings Table\n\nAggregate all findings from the three lenses into a single table, sorted by severity:\n\n| # | File | Line(s) | Lens | Issue | Severity | Auto-fixable? |\n|---|------|---------|------|-------|----------|---------------|\n| 1 | \\`src/foo.ts\\` | 42-55 | REUSE | Duplicates existing \\`formatDate\\` in \\`utils/dates.ts\\` | HIGH | Yes |\n| 2 | \\`src/bar.ts\\` | 18 | QUALITY | Empty catch block swallows network error | HIGH | Yes |\n| 3 | \\`src/baz.ts\\` | 92-94 | EFFICIENCY | Sequential awaits that could be parallel | MEDIUM | Yes |\n\nAfter the table:\n- **Summary**: \"Found X issues across Y files (Z auto-fixable)\"\n- **Recommendation**: \"Shall I fix the N auto-fixable issues? I'll apply them one at a time so you can review each change.\"\n\n---\n\n## Phase 4: FIX — Apply Auto-Fixable Issues\n\nIf the user approves (or for auto-fix items when user says \"fix all\"):\n\n1. Apply fixes **one at a time**, in order of severity (HIGH first)\n2. For each fix, explain:\n   - **What changed**: The specific edit made\n   - **Why**: Which lens finding this addresses\n   - **Confidence**: HIGH (mechanical replacement), MEDIUM (judgment call), LOW (suggest manual review)\n3. After each fix, verify it doesn't break surrounding code — check imports, types, and test compilation if applicable\n4. After all fixes are applied, present a summary:\n   - \"Applied X/Y auto-fixable changes. Z issues require manual review — see items #A, #B above.\"\n\n---\n\n## Anti-Pattern Quick Reference (flag these on sight)\n\n| Pattern | Why it's bad | Fix |\n|---------|-------------|-----|\n| Empty catch block | Hides errors, makes debugging impossible | Log or rethrow with context |\n| \\`console.log\\` in prod code | No structure, no levels, pollutes output | Use project logger or remove |\n| TODO without ticket | Untracked debt, never gets done | Add ticket ref or do it now |\n| Magic number \\`if (x > 86400)\\` | Unreadable, unmaintainable | \\`const SECONDS_PER_DAY = 86400\\` |\n| God function (50+ lines) | Hard to test, hard to understand | Extract sub-functions |\n| \\`any\\` type escape hatch | Defeats type safety | Use proper type or \\`unknown\\` |","allowed_tools":["bash","read-file","edit-file","grep","glob"],"enabled":true,"version":"1.0.0","category":"code-quality","when_to_use":"When the user asks to review code changes, check code quality, audit recent commits, or do a code review."},
  {"name":"schedule","description":"Schedule an agent to run a task on a recurring interval (e.g., 'every morning at 9am check for new issues').","prompt_template":"You are executing the /schedule skill. Task: {{ARGS}}\n\n## Scheduling Workflow\n\n### Step 1: Parse the Schedule\nExtract from the user's request:\n- **What**: The task to execute\n- **When**: The schedule (e.g., \"every 5 minutes\", \"daily at 9am\", \"weekdays at noon\")\n- **Who**: Which agent should run it (default: current agent)\n\nConvert the schedule to a cron expression:\n- \"every 5 minutes\" → */5 * * * *\n- \"daily at 9am\" → 0 9 * * *\n- \"weekdays at noon\" → 0 12 * * 1-5\n- \"every hour\" → 0 * * * *\n\n### Step 2: Confirm\nPresent the schedule to the user:\n\"I'll schedule [agent] to run '[task]' on this schedule:\n- Cron: [expression]\n- Next run: [computed]\n- Timezone: [user's timezone]\n\nProceed?\"\n\n### Step 3: Create\nUse the HTTP request tool to create the schedule via the control-plane API:\nPOST /api/v1/schedules\n{\n  \"agent_name\": \"[agent]\",\n  \"schedule\": \"[cron]\",\n  \"task\": \"[task description]\",\n  \"timezone\": \"[tz]\"\n}\n\n### Step 4: Confirm\nReport the created schedule ID and next execution time.","allowed_tools":["http-request"],"enabled":true,"version":"1.0.0","category":"automation"},
  {"name":"skillify","description":"Extract a repeatable process from this conversation into a reusable skill definition.","prompt_template":"You are executing the /skillify skill. Description: {{ARGS}}\n\n## Skill Extraction Interview\n\nI'll help you capture this process as a reusable skill. Let me ask a few questions:\n\n### Round 1: Identity\n- **Name**: What should this skill be called? (lowercase-kebab-case, e.g., \"deploy-to-prod\")\n- **Description**: One sentence describing what it does.\n- **When to use**: What trigger phrases should activate this skill?\n\n### Round 2: Steps\n- What are the high-level steps of this process?\n- What tools does each step need?\n- Are any steps parallelizable?\n\n### Round 3: Details\nFor each step:\n- What's the success criteria?\n- What are common failure modes?\n- Are there any prerequisites?\n\n### Round 4: Finalize\n- Are there edge cases or gotchas to document?\n- Should this skill be available to all agents or just specific ones?\n\nAfter the interview, I'll generate a skill definition and save it.\n\nRULES:\n- Ask one round of questions at a time. Wait for answers before proceeding.\n- Generate the skill with a detailed prompt_template that another agent can follow.\n- Include error handling and fallback instructions in the generated prompt.","allowed_tools":["read-file","write-file"],"enabled":true,"version":"1.0.0","category":"meta"},
  {"name":"verify","description":"Verify that a change works by running the agent's eval test cases against it.","prompt_template":"You are executing the /verify skill. What to verify: {{ARGS}}\n\n# Verify: Structured Verification Methodology\n\nYou verify that a code change does what it claims, handles edge cases, and doesn't break existing functionality. Follow all four phases. NEVER claim a test passes when it fails — accuracy is more important than a clean report.\n\n---\n\n## Phase 1: UNDERSTAND — Read the Change and Its Intent\n\n1. **Read the diff.** Run \\`git diff\\` (or \\`git diff HEAD~1\\` for committed changes) to see exactly what changed. Note every file, every added line, every removed line.\n2. **Read the full files.** Don't just read the diff — read the complete changed files to understand the surrounding context, invariants, and contracts.\n3. **Identify the intent.** What is this change supposed to do? Check:\n   - Commit message (if committed)\n   - PR description (if available)\n   - The user's stated goal\n   - Comments in the code\n4. **Identify the blast radius.** What could this change break?\n   - What other files import/depend on the changed code?\n   - What features use the changed code paths?\n   - Are there config changes that affect multiple environments?\n   - Use \\`grep\\` to find all callers/importers of changed exports\n\n---\n\n## Phase 2: PLAN TESTS — Design Verification for Each Change\n\nFor each meaningful change, plan tests across four dimensions:\n\n### Positive Tests (Happy Path)\n- Does the change achieve its stated goal?\n- Does it work with typical, expected input?\n- Does it produce the correct output/behavior?\n\n### Negative Tests (Edge Cases and Bad Input)\n- What happens with null/undefined/empty input?\n- What happens with malformed input?\n- What happens with extremely large input?\n- What happens with special characters (unicode, newlines, HTML entities)?\n- What happens at boundary values (0, -1, MAX_INT, empty string vs null)?\n\n### Regression Tests (Blast Radius)\n- Do existing features that depend on the changed code still work?\n- Do the existing test suites pass?\n- Are there integration points that might break (API contracts, event shapes, DB schemas)?\n\n### Performance Tests (if change is non-trivial)\nOnly run performance checks if the change is >10 lines or touches a hot path:\n- Is the change measurably slower? (benchmark before/after)\n- Does it add new allocations to a hot loop?\n- Does it add new I/O to a synchronous path?\n\n### What to Verify by Change Type\n\n| Change Type | Must Verify | Also Verify |\n|------------|-------------|-------------|\n| **Bug fix** | Fix works + original bug report scenario | No regression in related features |\n| **New feature** | Feature works end-to-end | No regression + feature is discoverable |\n| **Refactor** | Behavior is unchanged (same inputs → same outputs) | Performance is not degraded |\n| **Config change** | Works in target environment | Works in ALL environments (dev, staging, prod) |\n| **Dependency update** | Build succeeds + tests pass | No breaking API changes in the dependency |\n| **Security fix** | Vulnerability is patched | No new attack surface introduced |\n| **Performance fix** | Measurable improvement | No correctness regression |\n\n---\n\n## Phase 3: EXECUTE — Run Tests Systematically\n\n### Step 3.1: Run Existing Test Suite First\n\nAlways start with the project's own tests:\n1. Check for test scripts: \\`package.json\\` scripts, \\`Makefile\\` targets, \\`pytest.ini\\`, \\`jest.config\\`, \\`.github/workflows\\`\n2. Run the full test suite: \\`npm test\\`, \\`bun test\\`, \\`pytest\\`, \\`go test ./...\\`, etc.\n3. If the full suite is slow, run only the tests related to changed files first, then the full suite\n4. Record: which tests ran, how many passed, how many failed, how long it took\n\n### Step 3.2: Run Type Checking (if applicable)\n\n- TypeScript: \\`npx tsc --noEmit\\`\n- Python: \\`mypy\\` or \\`pyright\\`\n- Go: \\`go vet ./...\\`\n- Record any type errors introduced by the change\n\n### Step 3.3: Run Linting (if applicable)\n\n- Check for lint config: \\`.eslintrc\\`, \\`biome.json\\`, \\`.pylintrc\\`, \\`golangci-lint\\`\n- Run the linter on changed files\n- Record any new warnings/errors\n\n### Step 3.4: Ad-Hoc Scenarios (if no existing tests cover the change)\n\nIf the project lacks tests for the changed area, create ad-hoc verification:\n\n**Unit verification:**\n- Call the changed function directly with test inputs\n- Check return values match expectations\n- Check error cases throw/return appropriate errors\n\n**Integration verification:**\n- Start the service/server if applicable\n- Hit the affected endpoint/route with test requests\n- Verify responses match expectations\n\n**E2E verification:**\n- Walk through the full user flow that exercises the change\n- Check that the change is visible/functional from the user's perspective\n\n### Step 3.5: Manual Inspection\n\nEven if all automated tests pass, manually inspect:\n- Does the diff contain any debug code (\\`console.log\\`, \\`debugger\\`, \\`print\\`)? Flag it.\n- Does the diff contain any hardcoded values that should be configurable? Flag it.\n- Does the diff contain any commented-out code? Flag it.\n- Does the diff handle all error paths, or does it only handle the happy path?\n\n---\n\n## Phase 4: REPORT — Honest, Structured Results\n\n### Test Results Table\n\n| # | Test | Type | Expected | Actual | Status |\n|---|------|------|----------|--------|--------|\n| 1 | Unit tests pass | Existing | 0 failures | 0 failures | PASS |\n| 2 | Type check clean | Existing | 0 errors | 2 errors | FAIL |\n| 3 | Login still works | Regression | 200 OK | 200 OK | PASS |\n| 4 | Empty input handled | Edge case | Returns null | Throws TypeError | FAIL |\n\n### For Each Failure\n\nInclude ALL of the following:\n1. **What failed**: The exact test or scenario\n2. **Expected**: What should have happened\n3. **Actual**: What actually happened (include the exact error output)\n4. **Root cause**: Why it failed (if determinable)\n5. **Suggested fix**: How to fix it (if obvious)\n\n### Summary\n\n- **Verdict**: PASS (all tests pass, change is safe to ship) | FAIL (issues found, see details) | PARTIAL (some concerns, but shippable with caveats)\n- **Confidence**: HIGH (comprehensive test coverage) | MEDIUM (some gaps in coverage) | LOW (minimal testing available)\n- **Risks**: Any remaining concerns even if tests pass (e.g., \"No load testing done\", \"Cannot verify in production-like environment\")\n- **Recommendations**: Next steps (e.g., \"Add a test for the empty-input edge case before merging\")\n\n---\n\n## Rules\n\n- **NEVER claim passing when failing.** If a test fails, report it honestly. A false green is worse than a real red.\n- **NEVER skip verification steps silently.** If you can't run a test (missing dependency, no test config), say so explicitly.\n- **Test the change, not your assumptions.** Run the actual code — don't just read it and say \"this looks correct.\"\n- **Include exact output.** When reporting failures, include the actual error message, not a paraphrase.\n- **Verify the negative.** It's not enough that the right thing happens — verify that the wrong thing doesn't happen.","allowed_tools":["bash","read-file","http-request"],"enabled":true,"version":"1.0.0","category":"testing","when_to_use":"When the user asks to verify a change works, run tests, or check for regressions."},
];

export const BUNDLED_SKILLS_BY_NAME: Record<string, Skill> =
  Object.fromEntries(BUNDLED_SKILLS.map((s) => [s.name, s]));
