$kind: http-request
name: Get RAG Status
method: GET
url: '{{base_url}}/api/v1/rag/{{agent_name}}/status'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: agent_name
    value: '{{agent_name}}'
