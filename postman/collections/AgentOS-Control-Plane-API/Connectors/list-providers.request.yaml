$kind: http-request
name: List Connector Providers
method: GET
url: '{{base_url}}/api/v1/connectors/providers'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
