$kind: http-request
name: Reject Evolution Proposal
method: POST
url: '{{base_url}}/api/v1/evolve/{{agent_name}}/proposals/{{proposal_id}}/reject'
order: 6000
pathVariables:
  - key: agent_name
    value: '{{agent_name}}'
  - key: proposal_id
    value: '{{proposal_id}}'
