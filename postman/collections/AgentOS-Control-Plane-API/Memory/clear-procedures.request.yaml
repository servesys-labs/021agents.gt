$kind: http-request
name: Clear Procedures
method: DELETE
url: '{{base_url}}/api/v1/memory/{{agent_name}}/procedures'
order: 9000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: agent_name
    value: '{{agent_name}}'
