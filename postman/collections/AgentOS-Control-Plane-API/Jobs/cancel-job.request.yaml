$kind: http-request
name: Cancel Job
method: POST
url: '{{base_url}}/api/v1/jobs/{{job_id}}/cancel'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: job_id
    value: '{{job_id}}'
