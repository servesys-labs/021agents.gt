$kind: http-request
name: Rotate Secrets
method: POST
url: '{{base_url}}/api/v1/secrets-rotation/rotate'
order: 1000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "secret_names": ["DB_PASSWORD", "API_KEY"],
      "dry_run": false
    }
