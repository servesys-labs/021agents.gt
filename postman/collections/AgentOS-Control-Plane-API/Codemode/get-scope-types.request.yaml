$kind: http-request
name: Get Scope TypeScript Types
method: GET
url: '{{base_url}}/api/v1/codemode/types/{{scope}}'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: scope
    value: agent
