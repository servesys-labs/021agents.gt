$kind: http-request
name: List Guardrail Policies
method: GET
url: '{{base_url}}/api/v1/guardrails/policies'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
