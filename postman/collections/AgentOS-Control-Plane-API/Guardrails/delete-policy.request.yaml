$kind: http-request
name: Delete Guardrail Policy
method: DELETE
url: '{{base_url}}/api/v1/guardrails/policies/{{policy_id}}'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: policy_id
    value: '{{policy_id}}'
