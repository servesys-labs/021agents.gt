$kind: http-request
name: Cancel A2A Task
method: POST
url: '{{base_url}}/api/v1/a2a/tasks/{{task_id}}/cancel'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: task_id
    value: '{{task_id}}'
