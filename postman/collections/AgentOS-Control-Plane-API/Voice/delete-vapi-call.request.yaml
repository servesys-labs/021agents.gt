$kind: http-request
name: Delete Vapi Call
method: DELETE
url: '{{base_url}}/api/v1/voice/vapi/calls/{{call_id}}'
order: 7000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: call_id
    value: '{{call_id}}'
