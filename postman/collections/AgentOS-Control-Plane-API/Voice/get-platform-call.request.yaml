$kind: http-request
name: Get Platform Call
method: GET
url: '{{base_url}}/api/v1/voice/{{platform}}/calls/{{call_id}}'
order: 12000
pathVariables:
  - key: platform
    value: '{{platform}}'
  - key: call_id
    value: '{{call_id}}'
