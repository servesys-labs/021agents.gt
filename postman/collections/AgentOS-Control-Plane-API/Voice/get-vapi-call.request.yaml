$kind: http-request
name: Get Vapi Call Detail
method: GET
url: '{{base_url}}/api/v1/voice/vapi/calls/{{call_id}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: call_id
    value: '{{call_id}}'
