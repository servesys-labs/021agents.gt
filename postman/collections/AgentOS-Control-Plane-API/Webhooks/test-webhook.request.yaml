$kind: http-request
name: Test Webhook Delivery
method: POST
url: '{{base_url}}/api/v1/webhooks/{{webhook_id}}/test'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: webhook_id
    value: '{{webhook_id}}'
