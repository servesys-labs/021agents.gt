$kind: http-request
name: Rotate Webhook Secret
method: POST
url: '{{base_url}}/api/v1/webhooks/{{webhook_id}}/rotate-secret'
order: 8000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: webhook_id
    value: '{{webhook_id}}'
