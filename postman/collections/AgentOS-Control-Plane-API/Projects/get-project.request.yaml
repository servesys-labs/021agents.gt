$kind: http-request
name: Get Project
method: GET
url: '{{base_url}}/api/v1/projects/{{project_id}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: project_id
    value: '{{project_id}}'
