$kind: http-request
name: Get Canvas Layout
method: GET
url: '{{base_url}}/api/v1/projects/{{project_id}}/canvas-layout'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: project_id
    value: '{{project_id}}'
