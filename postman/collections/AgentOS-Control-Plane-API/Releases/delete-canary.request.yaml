$kind: http-request
name: Delete Canary Split
method: DELETE
url: '{{base_url}}/api/v1/releases/{{agent_name}}/canary'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: agent_name
    value: '{{agent_name}}'
