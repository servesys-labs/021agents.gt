$kind: http-request
name: Validate Canary Split
method: POST
url: '{{base_url}}/api/v1/releases/{{agent_name}}/canary/validate'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: agent_name
    value: '{{agent_name}}'
