$kind: http-request
name: Get Canary Split
method: GET
url: '{{base_url}}/api/v1/releases/{{agent_name}}/canary'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: agent_name
    value: '{{agent_name}}'
