$kind: http-request
name: Test Alert
method: POST
url: '{{base_url}}/api/v1/alerts/{{alert_id}}/test'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: alert_id
    value: '{{alert_id}}'
