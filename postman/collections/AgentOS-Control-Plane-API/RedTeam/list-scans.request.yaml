$kind: http-request
name: List Scans
method: GET
url: '{{base_url}}/api/v1/redteam/scans'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
queryParams:
  - key: limit
    value: '20'
