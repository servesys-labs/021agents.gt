$kind: http-request
name: Get Scan
method: GET
url: '{{base_url}}/api/v1/redteam/scans/{{scan_id}}'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: scan_id
    value: '{{scan_id}}'
