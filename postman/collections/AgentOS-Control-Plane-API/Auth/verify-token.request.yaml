$kind: http-request
name: Verify Token
method: POST
url: '{{base_url}}/api/v1/auth/token/verify'
order: 11000
auth:
  type: noauth
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "token": "{{api_token}}"
    }
