$kind: http-request
name: List Org Members
method: GET
url: '{{base_url}}/api/v1/orgs/{{org_id}}/members'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: org_id
    value: '{{org_id}}'
