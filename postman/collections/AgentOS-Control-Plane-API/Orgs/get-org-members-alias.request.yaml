$kind: http-request
name: Get Org Members (alias)
method: GET
url: '{{base_url}}/api/v1/org/{{org_id}}/members'
order: 11000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: org_id
    value: '{{org_id}}'
