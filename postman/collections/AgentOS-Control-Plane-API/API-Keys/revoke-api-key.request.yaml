$kind: http-request
name: Revoke API Key
method: DELETE
url: '{{base_url}}/api/v1/api-keys/{{key_id}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: key_id
    value: '{{key_id}}'
