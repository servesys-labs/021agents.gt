$kind: http-request
name: Apply Retention
method: POST
url: '{{base_url}}/api/v1/retention/apply'
order: 4000
headers:
  - key: Content-Type
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
body:
  type: json
  content: |-
    {
      "dry_run": false
    }
