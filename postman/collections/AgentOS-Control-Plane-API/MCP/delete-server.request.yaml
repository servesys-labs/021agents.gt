$kind: http-request
name: Delete MCP Server
method: DELETE
url: '{{base_url}}/api/v1/mcp/servers/{{server_id}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{api_token}}'
pathVariables:
  - key: server_id
    value: '{{server_id}}'
