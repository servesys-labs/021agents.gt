$kind: http-request
name: Delete Conversation (Public)
method: DELETE
url: '{{base_url}}/v1/agents/{{agent_name}}/conversations/{{session_id}}'
order: 8000
pathVariables:
  - key: agent_name
    value: '{{agent_name}}'
  - key: session_id
    value: '{{session_id}}'
